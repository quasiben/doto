
package: doto

platform:
  - linux-64

engine:
   - python=2
   - python=3

before_script:
  - python -V
  - conda install -q six requests pycrypto

script:
  - python setup.py install
  - python -c 'import doto';

