package:
  name: doto
  version: 0.3

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - requests
    - pycrypto
    - six 

test:
  # Python imports
  imports:
    - doto

about:
  home: https://github.com/quasiben/doto
  license: BSD

